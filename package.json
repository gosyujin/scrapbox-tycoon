{
  "name": "scrapbox-tycoon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && node scripts/write-build-info.mjs && node scripts/cache-bust-imports.mjs",
    "watch": "tsc --watch",
    "dev": "npm run build && node server.js"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
